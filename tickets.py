tickets = int(input("введите количство билетов"))
age = int(input("Введите возраст"))
cost = 0
print(type(age))
for(i in range(tickets)
  if age >= 18 & age <= 25:
    cost + 990 * tickets
    elif age > 25:
      cost + 1390 * tickets
    else:
      cost = 0
    if tickets >= 3:
      cost = - cost / 100 * 10
 
print("total cost = ", cost)